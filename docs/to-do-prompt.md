Task: Install install fabric js (Latest stable) in this project, then build a minimal canvas that displays
at the correct size based on template dimensions. Do ONLY what is listed below —
no toolbar, no shapes, no extra features.

STEP 1 — Install
Run: npm install fabric js (Latest stable)
Confirm it's added to package.json under dependencies.

STEP 2 — Create the canvas component
File: resources/js/components/Editor (group existing components here as well and update the imports where these imports are added)

- Import Fabric like this: import * as fabric from 'fabric';
- Props (TypeScript interface):
  interface CanvasEditorProps {
  widthPx: number;
  heightPx: number;
  unit: 'px' | 'mm' | 'in';
  }
- Unit conversion (only if unit !== 'px'), using 96 DPI:
    - 1 in = 96 px
    - 1 mm = 96 / 25.4 px
      Convert widthPx and heightPx to final pixel values BEFORE using them.
- Use useRef<HTMLCanvasElement>(null) for the <canvas> element.
- Use useRef<fabric.Canvas | null>(null) to store the Fabric canvas instance across renders.
- In useEffect:
    - Guard: if the canvas ref is null, return early.
    - Create: new fabric.Canvas(canvasRef.current, { width: convertedWidth, height: convertedHeight, backgroundColor: '#f5f5f5' })
    - Store the instance in the fabric canvas ref.
    - Return a cleanup function that calls fabricCanvasRef.current?.dispose() and sets
      the ref back to null. This is required so it doesn't break on Inertia page
      navigation or React StrictMode double-mounting.
- The <canvas> element should have a visible CSS border (e.g. border: 1px solid #ccc)
  so the canvas edges are visible even though the background is light gray.
- Export as a default export function component.

Do not add: toolbar, shapes, text objects, event listeners, or state management.
Just: correct-size canvas, gray background, visible border. That's the entire scope
of this component for now.

STEP 3 — Create a test page to view it
File: resources/js/pages/editor/templates/create.tsx

- This is an Inertia page component. Default export a function component.
- Import CanvasEditor from '@/Components/Editor/CanvasEditor'
  (check the existing tsconfig/vite alias — if '@/' is not configured, use the correct
  relative path instead, e.g. '../../../Components/Editor/CanvasEditor')
- Hardcode these props and pass them to <CanvasEditor />:
  widthPx: 638
  heightPx: 1013
  unit: 'px'
  (This is a standard CR80 ID card, 2.13in x 3.38in at 300 DPI — realistic size for
  testing, but treat these numbers as placeholder data only, not from any database yet.)
- Wrap it in a simple div with some padding so it's not flush against the browser edge.
- Do not add a layout wrapper, navigation, or auth check yet.

STEP 4 — Add the route
File: routes/web.php (or wherever Inertia page routes are currently defined in this project)

- Add: GET /editor/templates/create
- It should call Inertia::render('Editor/Templates/Create') and return the result.
- Do NOT add any middleware (no auth, no role check) yet — this is a temporary
  test route just to view the canvas in the browser.
- If a TemplateController does not exist yet, put this route as a closure directly
  in web.php for now — do not create a full controller for this step.

VERIFY WHEN DONE:

- Run the dev server, visit /editor/templates/create in the browser.
- Confirm you see a light gray rectangle with a visible border at roughly a
  portrait ID-card aspect ratio (taller than it is wide).
- Confirm no console errors related to Fabric or React.

Stop after this is working. Do not proceed to add more features unless asked.

the prompt here is generated by an AI that do not understand the file structure and name convetion this project is built with. you know the file structure better so explicitly handle them
